# Machine-local skills_doctor config: per-machine project list, never committed.
# The committed skills_doctor.config.example.json documents the schema.
scripts/skills_doctor.config.json

# PAC eval workspace (iteration artifacts, not committed)
meta-skill-feedback-workspace/
